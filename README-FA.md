<div align="center">

  <a href="README.md">
    <img src="https://img.shields.io/badge/Language-English-blue?style=for-the-badge" alt="English">
  </a>
  
  <a href="README-FA.md">
    <img src="https://img.shields.io/badge/Language-Persian_(فارسی)-green?style=for-the-badge" alt="Persian">
  </a>

  <a href="installer/README.md">
    <img src="https://img.shields.io/badge/Guide-Install_on_Hosting_(cPanel)-orange?style=for-the-badge&logo=cpanel&logoColor=white" alt="راهنمای نصب روی هاست">
  </a>

</div>
<br>

<div dir="rtl">

# ربات میرزا پرو و اسکریپت مدیریت
این ریپازیتوری شامل پروژه اوپن‌سورس **ربات میرزا پرو** است که همراه با یک اسکریپت قدرتمند برای نصب و مدیریت خودکار ارائه شده است.

<p align="center">
  <img src="https://raw.githubusercontent.com/KillHosein/MirzaFutured/main/Banner.jpg" alt="بنر ربات میرزا پرو" width="600"/>
</p>

## 🎉 پروژه نسخه پرو، اوپن‌سورس شد!
نسخه‌ی **Pro** این پروژه اکنون به‌صورت **اوپن‌سورس** در دسترس است!  
از مشارکت، پیشنهادها و همکاری شما برای توسعه‌ی بهتر پروژه استقبال می‌کنیم 🤝

#### 📜 درباره این اسکریپت
این ریپازیتوری یک اسکریپت قدرتمند bash برای ساده‌سازی تمام چرخه عمر ربات میرزا پرو ارائه می‌دهد. از نصب کامل گرفته تا پشتیبان‌گیری منظم و حذف آسان، این اسکریپت همه کارها را برای شما انجام می‌دهد.

#### ✨ قابلیت‌ها
*   **نصب خودکار:** نصب ربات، وب‌سرور (آپاچی)، PHP، دیتابیس MySQL و امن‌سازی با گواهی SSL رایگان (Let's Encrypt).
*   **مدیریت پشتیبان‌گیری:**
    *   ایجاد بکاپ دستی از دیتابیس (`.sql`) و فایل‌های سورس (`.tar.gz`).
    *   ارسال مستقیم بکاپ‌ها به یک کانال یا چت خصوصی تلگرام.
    *   پیکربندی زمان‌بندی بکاپ خودکار (روزانه، هفتگی، سفارشی).
*   **حذف آسان:** حذف کامل تمام اجزای ربات، شامل فایل‌ها، دیتابیس، تنظیمات وب‌سرور و کرون‌جاب‌ها.
*   **تمدید گواهی SSL:** یک دستور ساده برای تمدید گواهی SSL.
*   **منوی تعاملی:** یک منوی کاربرپسند برای دسترسی به تمام قابلیت‌ها.

#### 🚀 روش نصب
برای نصب ربات با استفاده از اسکریپت مدیریت، دستور زیر را به عنوان کاربر root اجرا کنید:

```bash
bash <(curl -s https://raw.githubusercontent.com/KillHosein/MirzaFutured/main/install.sh)
```

**گزینه‌های منو:**
1.  **نصب / نصب مجدد ربات:** فرآیند نصب کامل را اجرا می‌کند.
2.  **مدیریت پشتیبان‌گیری:** وارد زیرمنوی بکاپ می‌شود.
3.  **تمدید گواهی SSL:** گواهی SSL را تمدید می‌کند.
4.  **حذف ربات:** ربات و تمام اجزای آن را به طور کامل حذف می‌کند.
5.  **خروج:** از اسکریپت خارج می‌شود.

#### ⚠️ نکات مهم برای کاربران
برای اطمینان از نصب موفق، لطفاً اطلاعات زیر را به درستی وارد کنید:

*   **دامنه:** دامنه یا ساب‌دامین خود را **بدون** `http://` یا `https://` وارد کنید (مثال: `bot.yourdomain.com`).
*   **توکن ربات تلگرام:** مطمئن شوید که توکن کامل و صحیح را از BotFather کپی کرده‌اید.
*   **آیدی عددی ادمین:** این مقدار باید **آیدی عددی** شما باشد، نه نام کاربری. آن را از ربات‌هایی مانند `@userinfobot` دریافت کنید.
*   **آیدی چت بکاپ:** برای کانال‌های خصوصی، این آیدی معمولاً با `-100` شروع می‌شود. ربات شما باید در آن کانال **ادمین** باشد و دسترسی ارسال پیام را داشته باشد.
*   **نسخه پنل مرزبان:** اگر از پنل مرزبان نسخه **0.8.4 یا پایین‌تر** استفاده می‌کنید، باید به سوال `Are you using the NEW Marzban panel?` پاسخ **`no`** بدهید. برای نسخه‌های جدیدتر، پاسخ **`yes`** را وارد کنید.

---

## مینی‌اپ تلگرام (Web App)
این پروژه شامل یک مینی‌اپ (وب‌اپ داخل تلگرام) در مسیر `app/` است که از **Telegram Web Apps** و **Bot API** استفاده می‌کند و از طریق APIهای `api/verify.php` و `api/miniapp.php` به هسته ربات متصل می‌شود.

### فعال‌سازی در BotFather
برای فعال‌سازی مینی‌اپ:
1. در BotFather روی ربات خود دستور `/newapp` (یا بخش Web App) را اجرا کنید.
2. `Web App URL` را روی آدرس زیر تنظیم کنید:
   - `https://YOUR_DOMAIN/app/`

### نحوه کار احراز هویت
- در سمت کلاینت، `initData` از `Telegram.WebApp.initData` دریافت می‌شود.
- در سمت سرور، فایل `api/verify.php` با استفاده از توکن ربات (`$APIKEY` در `config.php`) امضای `initData` را بررسی می‌کند و سپس یک `token` برای کاربر صادر می‌کند.
- در ادامه تمام درخواست‌های مینی‌اپ به `api/miniapp.php` با هدر `Authorization: Bearer <token>` ارسال می‌شوند.

### قابلیت‌های مینی‌اپ
*   **پروفایل کاربر:** نمایش و ثبت اطلاعات پایه (شماره، اطلاعات اختیاری).
*   **کیف پول:** نمایش موجودی، ایجاد درخواست کارت‌به‌کارت، ثبت رسید و مشاهده تاریخچه پرداخت‌ها.
*   **خرید سرویس:** نمایش لیست سرویس‌ها، سبد خرید و خرید با موجودی، فعال‌سازی خودکار.
*   **پنل ادمین (محدود):** نمایش آمار کلیدی برای ادمین‌های تعریف‌شده در جدول `admin`.

### پیش‌نیازها
*   `config.php` باید با اطلاعات دیتابیس، دامنه و `APIKEY` (توکن BotFather) تکمیل شود.
*   وب‌سرور باید مسیر `app/` را به عنوان فایل‌های استاتیک سرو کند و مسیر `api/` قابل دسترسی باشد.

---

### 💖 حمایت از توسعه‌دهندگان
این پروژه حاصل تلاش چندین توسعه‌دهنده است. لطفاً از آن‌ها حمایت کنید.

#### حمایت از پروژه اصلی ربات میرزا پرو (mahdiMGF2)
برای حمایت از سازنده اصلی پروژه ربات:

👈 [حمایت از طریق NowPayments](https://nowpayments.io/donation/permiumbotmirza)

🙏 از حمایت شما از پروژه‌های متن‌باز سپاسگزاریم! 🚀

#### حمایت از اسکریپت مدیریت (ExPLoSiVe1988)
اگر این اسکریپت مدیریت برای شما مفید بوده است، می‌توانید برای کمک به نگهداری و ارائه به‌روزرسانی‌های آینده، از توسعه‌دهنده آن حمایت کنید:

| ارز دیجیتال                | آدرس                                         |
|:---------------------------|:---------------------------------------------|
| 🟣 **اتریوم (ETH - ERC20)**   | `0x157F3Eb423A241ccefb2Ddc120eF152ce4a736eF` |
| 🔵 **ترون (TRX - TRC20)**      | `TEdu5VsNNvwjCRJpJJ7zhjXni8Y6W5qAqk`         |
| 🟢 **تتر (USDT - TRC20)**     | `TN3cg5RM5JLEbnTgK5CU95uLQaukybPhtR`         |

---

⭐ لطفاً پروژه را **Star** کنید تا دیگران هم آن را پیدا کنند!

---

### 👥 سازندگان
*   **پروژه اصلی (ربات میرزا پرو):** [mahdiMGF2](https://github.com/mahdiMGF2)
*   **توسعه‌دهنده اسکریپت مدیریت:**
    *   تلگرام: [@H_ExPLoSiVe](https://t.me/H_ExPLoSiVe)
    *   کانال: [@Botgineer](https://t.me/Botgineer)

</div>
