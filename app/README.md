# Mirza Web App - Professional Telegram Web Application

یک اپلیکیشن وب حرفه‌ای برای تلگرم با قابلیت‌های کامل مدیریت کاربران، تنظیمات پیشرفته و امنیت بالا.

## ✨ ویژگی‌ها

### 🎯 قابلیت‌های اصلی
- **ورود امن با تلگرم**: احراز هویت کامل با Web App API تلگرم
- **پروفایل کاربری**: مدیریت کامل اطلاعات کاربر
- **تنظیمات پیشرفته**: زبان، اعلان‌ها، حریم خصوصی و موارد دیگر
- **آمار و گزارش‌ها**: مشاهده فعالیت‌ها و آمار استفاده
- **خروجی داده‌ها**: امکان خروجی گرفتن از اطلاعات کاربر

### 🔐 امنیت
- **CSRF Protection**: حفاظت در برابر حملات CSRF
- **Rate Limiting**: محدودیت درخواست‌ها برای جلوگیری از اسپم
- **Input Sanitization**: تصفیه کامل ورودی‌ها
- **Session Management**: مدیریت امن جلسات کاربری
- **Content Security Policy**: سیاست‌های امنیتی محتوا

### 🎨 رابط کاربری
- **طراحی واکنش‌گرا**: سازگار با تمام دستگاه‌ها
- **تم تلگرم**: استفاده از رنگ‌های تم تلگرم
- **انیمیشن‌های حرفه‌ای**: افکت‌های بصری زیبا
- **پشتیبانی از RTL**: کامل برای زبان فارسی
- **فونت وزیر**: استفاده از فونت فارسی حرفه‌ای

### 📊 مدیریت داده‌ها
- **MySQL Database**: پایگاه داده قدرتمند و مقیاس‌پذیر
- **Caching System**: سیستم کشینگ برای عملکرد بهتر
- **Data Export**: خروجی CSV و JSON
- **Activity Logging**: ثبت کامل فعالیت‌ها
- **Backup System**: سیستم پشتیبان‌گیری خودکار

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- PHP 8.0 یا بالاتر
- MySQL 5.7 یا بالاتر
- Apache/Nginx با mod_rewrite
- SSL Certificate (توصیه شده)

### مراحل نصب

1. **دانلود پروژه**:
```bash
git clone https://github.com/your-username/mirza-web-app.git
cd mirza-web-app/app
```

2. **تنظیمات پایگاه داده**:
```bash
# کپی فایل محیطی
cp .env.example .env

# ویرایش فایل .env و تنظیم اطلاعات دیتابیس
nano .env
```

3. **ایجاد پایگاه داده**:
```bash
# اجرای اسکریپت ایجاد جداول
php utils/initialize_database.php
```

4. **تنظیمات تلگرم**:
- بات خود را در [@BotFather](https://t.me/BotFather) بسازید
- توکن بات را در فایل `.env` وارد کنید
- Webhook را تنظیم کنید

5. **تنظیمات وب‌سرور**:
```apache
# برای Apache - فایل .htaccess موجود است
# برای Nginx - اضافه کردن این تنظیمات:
location / {
    try_files $uri $uri/ /index.php?$query_string;
}
```

## 📁 ساختار پوشه‌ها

```
app/
├── assets/                 # فایل‌های استاتیک
│   ├── css/               # فایل‌های CSS
│   ├── js/                # فایل‌های JavaScript
│   └── images/            # تصاویر
├── components/             # کامپوننت‌های PHP
│   ├── header.php         # هدر اصلی
│   ├── footer.php         # فوتر اصلی
│   ├── home.php           # صفحه اصلی
│   ├── profile.php        # صفحه پروفایل
│   ├── settings.php       # صفحه تنظیمات
│   └── error.php          # صفحه خطا
├── config/                 # فایل‌های پیکربندی
│   └── config.php         # پیکربندی اصلی
├── includes/               # کلاس‌ها و توابع
│   ├── Database.php       # کلاس پایگاه داده
│   ├── SecurityManager.php # مدیریت امنیت
│   ├── TelegramWebApp.php  # کلاس تلگرم Web App
│   └── UserManager.php     # مدیریت کاربران
├── api/                    # API endpoints
│   └── APIHandler.php     # هندلر API
├── utils/                  # ابزارهای کمکی
│   ├── Utils.php          # توابع کاربردی
│   └── initialize_database.php # اسکریپت ایجاد دیتابیس
├── fonts/                  # فونت‌ها
└── index.php              # فایل اصلی ورودی
```

## 🔧 پیکربندی

### فایل `.env`
```env
# Application Settings
APP_NAME="Mirza Web App"
APP_URL="https://your-domain.com"
ENVIRONMENT="development"

# Database Configuration
DB_HOST="localhost"
DB_NAME="mirza_db"
DB_USER="root"
DB_PASS="your-password"

# Telegram Bot Configuration
BOT_TOKEN="YOUR_BOT_TOKEN_HERE"
WEBHOOK_URL="https://your-domain.com/webhook.php"

# Security Settings
SECRET_KEY="your-secret-key-here"
JWT_SECRET="your-jwt-secret-here"
ENCRYPTION_KEY="your-encryption-key-here"
```

## 📋 API Documentation

### Authentication
```http
GET /app/index.php?action=api&endpoint=user
Authorization: Bearer {token}
```

### Endpoints

#### User Information
```http
GET /app/index.php?action=api&endpoint=user
```

#### User Statistics
```http
GET /app/index.php?action=api&endpoint=stats
```

#### User Settings
```http
GET /app/index.php?action=api&endpoint=settings
POST /app/index.php?action=api&endpoint=settings
```

#### Activity Log
```http
GET /app/index.php?action=api&endpoint=activity
```

#### Data Export
```http
GET /app/index.php?action=api&endpoint=export
```

## 🎨 سفارشی‌سازی

### تغییر تم رنگی
در فایل `assets/css/style.css` می‌توانید متغیرهای CSS را تغییر دهید:

```css
:root {
    --tg-theme-button-color: #your-color;
    --tg-theme-text-color: #your-color;
    --tg-theme-bg-color: #your-color;
}
```

### افزودن زبان جدید
1. فایل‌های ترجمه را در `components/` اضافه کنید
2. در `config/config.php` زبان جدید را تعریف کنید
3. در `settings.php` گزینه زبان را به‌روزرسانی کنید

### افزودن قابلیت جدید
1. کلاس جدید را در `includes/` بسازید
2. API endpoint را در `api/APIHandler.php` اضافه کنید
3. صفحه جدید را در `components/` بسازید

## 🔍 خطایابی

### مشکلات رایج

1. **خطای 404 در API**:
   - بررسی کنید که mod_rewrite فعال باشد
   - فایل `.htaccess` را بررسی کنید

2. **خطای اتصال به دیتابیس**:
   - اطلاعات دیتابیس در `.env` را بررسی کنید
   - اطمینان حاصل کنید که MySQL در حال اجراست

3. **خطای تلگرم Web App**:
   - توکن بات را بررسی کنید
   - Webhook را تنظیم کنید
   - دامنه باید با HTTPS باشد

### لاگ‌ها
لاگ‌ها در فایل `logs/app.log` ذخیره می‌شوند:
```bash
tail -f logs/app.log
```

## 📈 عملکرد

### بهینه‌سازی‌ها
- **کشینگ**: استفاده از کش برای کاهش بار دیتابیس
- **فشرده‌سازی**: فشرده‌سازی CSS و JavaScript
- **Lazy Loading**: بارگذاری تنبل تصاویر
- **CDN**: پشتیبانی از CDN برای فایل‌های استاتیک

### مانیتورینگ
- استفاده از Google Analytics یا Mixpanel
- مانیتورینگ خطاها با Sentry یا Bugsnag
- بررسی عملکرد دیتابیس

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork پروژه را بگیرید
2. Branch جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را commit کنید (`git commit -m 'Add some amazing feature'`)
4. Branch را push کنید (`git push origin feature/amazing-feature`)
5. Pull Request باز کنید

## 📝 مجوز

این پروژه تحت مجوز MIT منتشر شده است. برای اطلاعات بیشتر فایل [LICENSE](LICENSE) را ببینید.

## 👥 تیم توسعه

- **Lead Developer**: [Your Name](https://github.com/your-username)
- **UI/UX Designer**: [Designer Name]
- **Backend Developer**: [Developer Name]

## 📞 پشتیبانی

برای پشتیبانی:
- تلگرم: [@your_support_bot](https://t.me/your_support_bot)
- ایمیل: support@your-domain.com
- وب‌سایت: https://your-domain.com

---

⭐ اگر این پروژه برای شما مفید بود، لطفاً آن را ستاره دهید!

**ساخته شده با ❤️ برای جامعه تلگرم**