# راهنمای یکپارچه‌سازی تنظیمات پایگاه داده

## 📋 خلاصه تغییرات

وب اپلیکیشن Mirza اکنون با تنظیمات پایگاه داده اصلی پروژه شما یکپارچه شده است. در ادامه تغییرات اعمال شده را توضیح می‌دهم:

## 🔗 یکپارچه‌سازی پایگاه داده

### 1. فایل‌های پیکربندی به‌روزرسانی شده:

#### `app/config/config.php`
- اکنون از فایل اصلی `config.php` استفاده می‌کند
- متغیرهای اصلی مانند `$APIKEY`، `$adminnumber`، `$domainhosts` و `$usernamebot` را به اشتراک می‌گیرد
- تنظیمات امنیتی و برنامه نیز حفظ شده‌اند

#### `app/includes/Database.php`
- از اتصال PDO موجود در فایل اصلی استفاده می‌کند
- در صورت عدم وجود اتصال، به صورت fallback اتصال جدید ایجاد می‌کند

#### `app/includes/AdminManager.php`
- از `ADMIN_TELEGRAM_ID` تعریف شده در فایل پیکربندی استفاده می‌کند
- دیگر نیازی به تعریف دستی شناسه ادمین ندارد

#### `app/webhook.php`
- ابتدا فایل اصلی پیکربندی را بارگذاری می‌کند
- سپس فایل‌های وب اپ را بارگذاری می‌کند
- این امر ensures که از همان اتصال پایگاه داده استفاده شود

## 📊 جداول پایگاه داده جدید

وب اپلیکیشن جداول زیر را اضافه می‌کند (در صورت عدم وجود):

1. **users** - اطلاعات کاربران تلگرم
2. **user_sessions** - نشست‌های کاربران
3. **user_settings** - تنظیمات شخصی کاربران
4. **notifications** - سیستم اعلان‌ها
5. **activity_log** - لاگ فعالیت‌ها
6. **system_settings** - تنظیمات سیستم
7. **backup_logs** - لاگ‌های پشتیبان‌گیری
8. **file_uploads** - فایل‌های آپلود شده

## 🚀 نصب و راه‌اندازی

### مرحله 1: اجرای اسکریپت ایجاد جداول
```bash
cd c:\Users\ASUS\Downloads\JC\Mirza-Pro-main\app
php utils\initialize_database_simple.php
```

### مرحله 2: تنظیم Webhook تلگرم
```bash
# در مرورگر یا با curl:
https://api.telegram.org/bot{YOUR_BOT_TOKEN}/setWebhook?url=https://your-domain.com/app/webhook.php
```

### مرحله 3: تست وب اپ
1. به آدرس `https://your-domain.com/app/index.php` بروید
2. از طریق تلگرم وارد شوید
3. از تمام قابلیت‌ها استفاده کنید

## 🔧 پیکربندی متغیرهای اصلی

در فایل اصلی `config.php` خود، مطمئن شوید که این متغیرها تنظیم شده‌اند:

```php
$APIKEY = 'YOUR_BOT_TOKEN';          // توکن بات تلگرم
$adminnumber = 'YOUR_ADMIN_ID';      // شناسه تلگرم ادمین
$domainhosts = 'https://your-domain.com'; // آدرس دامنه شما
$usernamebot = 'YOUR_BOT_USERNAME';  // نام کاربری بات بدون @
```

## 📁 ساختار فایل‌های نهایی

```
c:\Users\ASUS\Downloads\JC\Mirza-Pro-main/
├── config.php                    ← فایل اصلی پیکربندی شما
├── app/
│   ├── config/
│   │   └── config.php            ← استفاده از متغیرهای اصلی
│   ├── includes/
│   │   ├── Database.php          ← استفاده از اتصال موجود
│   │   ├── AdminManager.php      ← استفاده از ADMIN_TELEGRAM_ID
│   │   └── ...
│   ├── webhook.php               ← بارگذاری ابتدا فایل اصلی
│   └── utils/
│       └── initialize_database_simple.php
└── ...
```

## ✅ مزایای این رویکرد

1. **تک پایگاه داده**: فقط یک پایگاه داده برای مدیریت
2. **تنظیمات یکپارچه**: تمام تنظیمات در یک جا
3. **سازگاری کامل**: بدون تداخل در کد موجود شما
4. **قابلیت نگهداری آسان**: تغییرات فقط در یک نقطه
5. **امنیت بالا**: استفاده از اتصال PDO ایمن

## 🎯 قابلیت‌های قابل دسترس

پس از یکپارچه‌سازی، وب اپلیکیشن شما این قابلیت‌ها را دارد:

- ✅ ورود امن با Telegram Web App API
- ✅ پروفایل کاربری کامل
- ✅ تنظیمات پیشرفته
- ✅ سیستم اعلان‌ها
- ✅ آمار و گزارش‌ها
- ✅ پنل مدیریت حرفه‌ای
- ✅ API RESTful کامل
- ✅ امنیت پیشرفته (CSRF, Rate Limiting, etc.)
- ✅ طراحی واکنش‌گرا
- ✅ پشتیبانی از زبان فارسی

## 🔍 عیب‌یابی

### خطای اتصال به پایگاه داده:
```bash
# بررسی اتصال
php -r "require 'config.php'; var_dump(\$pdo);"
```

### خطای Webhook:
```bash
# بررسی لاگ‌ها
tail -f /path/to/your/error.log
```

### خطای 500 در وب اپ:
```bash
# بررسی فایل‌های لاگ در پوشه app/
ls app/logs/
```

## 📞 پشتیبانی

در صورت بروز هرگونه مشکل:
1. لاگ‌های سرور را بررسی کنید
2. تنظیمات فایل `config.php` را دوباره بررسی کنید
3. مطمئن شوید جداول به درستی ایجاد شده‌اند
4. Webhook تلگرم به درستی تنظیم شده باشد

---

**وب اپلیکیشن اکنون آماده استفاده است!** 🎉