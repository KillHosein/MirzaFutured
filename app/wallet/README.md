# سیستم کیف پول و تراکنشهای کارت به کارت

## معرفی
سیستم کیف پول و تراکنشهای کارت به کارت یک راهکار جامع برای افزایش موجودی کیف پول کاربران از طریق انتقال کارت به کارت است. این سیستم با استفاده از فناوریهای مدرن و امن، امکان مدیریت تراکنشها را برای کاربران و ادمینها فراهم میکند.

## ویژگیهای کلیدی

### 🔐 امنیت بالا
- اعتبارسنجی کامل اطلاعات کارت
- استفاده از الگوریتم Luhn برای اعتبارسنجی شماره کارت
- محدودیتهای امنیتی برای جلوگیری از سوءاستفاده
- لاگگیری کامل تمام تراکنشها

### 📱 رابط کاربری مدرن
- طراحی واکنشگرا و مدرن
- پشتیبانی از تم تلگرام
- رابط کاربری فارسی کاملاً بومیسازی شده
- تجربه کاربری بهینه

### ⚡ عملکرد سریع
- پردازش سریع تراکنشها
- بروزرسانی لحظهای موجودی
- اعلانهای فوری برای کاربران و ادمینها

### 📊 گزارشگیری جامع
- آمار و گزارشهای دقیق
- نمودارهای تحلیلی
- قابلیت خروجی گرفتن از دادهها

## ساختار فایلها

```
app/wallet/
├── database.php              # کلاس مدیریت پایگاه داده
├── card_to_card_manager.php # کلاس مدیریت تراکنشهای کارت به کارت
├── bot_interface.php        # رابط کاربری ربات تلگرام
├── api.php                  # API برای ارتباط با frontend
├── wallet.php               # صفحه اصلی کیف پول برای کاربران
├── wallet-service.js        # سرویسهای جاواسکریپت برای frontend
├── wallet-styles.css      # استایلهای CSS
├── config.php              # تنظیمات سیستم
├── card_to_card_admin.php # پنل مدیریت برای ادمینها
└── install.php             # اسکریپت نصب و راهاندازی
```

## نصب و راهاندازی

### 1. پیشنیازها
- PHP 7.4 یا بالاتر
- MySQL 5.7 یا بالاتر
- Apache/Nginx
- SSL Certificate (توصیه شده)

### 2. مراحل نصب

```bash
# 1. فایلها را در پوشه app/wallet کپی کنید
cd app/wallet

# 2. اسکریپت نصب را اجرا کنید
php install.php

# 3. تنظیمات را در config.php ویرایش کنید
```

### 3. تنظیمات پایگاه داده
اسکریپت نصب بهطور خودکار جدولهای مورد نیاز را ایجاد میکند:

#### جدول card_to_card_transactions
- `id`: شناسه یکتای تراکنش
- `user_id`: شناسه کاربر
- `transaction_id`: شناسه یکتای تراکنش
- `source_card_number`: شماره کارت مبدا (ماسک شده)
- `destination_card_number`: شماره کارت مقصد
- `amount`: مبلغ به ریال
- `amount_toman`: مبلغ به تومان
- `tracking_code`: شماره پیگیری
- `reference_number`: شماره مرجع
- `bank_name`: نام بانک
- `transaction_date`: تاریخ تراکنش
- `transaction_status`: وضعیت تراکنش
- `verification_status`: وضعیت تایید
- `verification_attempts`: تعداد تلاشهای تایید
- `verification_response`: پاسخ تایید
- `admin_notes`: یادداشتهای ادمین
- `created_at`: تاریخ ایجاد
- `updated_at`: تاریخ بروزرسانی
- `confirmed_at`: تاریخ تایید
- `rejected_at`: تاریخ رد
- `rejected_reason`: دلیل رد

#### جدول wallet_transactions
- `id`: شناسه یکتای تراکنش کیف پول
- `user_id`: شناسه کاربر
- `transaction_type`: نوع تراکنش (واریز، برداشت، بازگشت وجه، خرید، کمیسیون)
- `amount`: مبلغ
- `balance_before`: موجودی قبل
- `balance_after`: موجودی بعد
- `related_transaction_id`: شناسه تراکنش مرتبط
- `description`: توضیحات
- `reference_type`: نوع مرجع
- `reference_id`: شناسه مرجع
- `created_at`: تاریخ ایجاد

#### جدول bank_cards
- `id`: شناسه یکتا
- `user_id`: شناسه کاربر
- `card_number`: شماره کارت
- `bank_name`: نام بانک
- `card_owner_name`: نام صاحب کارت
- `is_verified`: وضعیت تایید
- `verification_date`: تاریخ تایید
- `usage_count`: تعداد دفعات استفاده
- `total_amount`: مجموع مبالغ
- `last_used_at`: آخرین استفاده
- `created_at`: تاریخ ایجاد
- `updated_at`: تاریخ بروزرسانی

## استفاده از API

### 1. دریافت موجودی کیف پول
```http
GET /app/wallet/api.php
Content-Type: application/json
Token: YOUR_TELEGRAM_TOKEN

{
    "action": "get_user_wallet_balance",
    "user_id": "123456789"
}
```

### 2. ایجاد تراکنش کارت به کارت
```http
POST /app/wallet/api.php
Content-Type: application/json
Token: YOUR_TELEGRAM_TOKEN

{
    "action": "create_card_to_card_transaction",
    "user_id": "123456789",
    "source_card_number": "6037991234567890",
    "destination_card_number": "6037991234567890",
    "amount": "500000",
    "bank_name": "بانک ملی ایران",
    "transaction_date": "2024-01-01 12:00:00"
}
```

### 3. دریافت تراکنشهای کاربر
```http
GET /app/wallet/api.php
Content-Type: application/json
Token: YOUR_TELEGRAM_TOKEN

{
    "action": "get_user_wallet_transactions",
    "user_id": "123456789",
    "limit": 50,
    "offset": 0
}
```

### 4. تایید تراکنش (ادمین)
```http
POST /app/wallet/api.php
Content-Type: application/json
Token: YOUR_TELEGRAM_TOKEN

{
    "action": "confirm_card_to_card_transaction",
    "transaction_id": "C2C1234567890123456789",
    "tracking_code": "1234567890",
    "reference_number": "REF123456",
    "admin_notes": "تراکنش تایید شد"
}
```

## تنظیمات سیستم

### تنظیمات عمومی
- `min_amount`: حداقل مبلغ تراکنش (پیشفرض: ۱۰٬۰۰۰ تومان)
- `max_amount`: حداکثر مبلغ تراکنش (پیشفرض: ۵۰٬۰۰۰٬۰۰۰ تومان)
- `processing_time_minutes`: زمان پردازش (پیشفرض: ۳۰ دقیقه)

### تنظیمات امنیتی
- `enable_rate_limiting`: فعالسازی محدودیت نرخ
- `max_attempts_per_hour`: حداکثر تلاش در ساعت
- `max_amount_per_day`: حداکثر مبلغ در روز
- `enable_fraud_detection`: فعالسازی تشخیص تقلب

### تنظیمات اعلانها
- `admin_notification_enabled`: فعالسازی اعلانهای ادمین
- `user_notification_enabled`: فعالسازی اعلانهای کاربر
- `notify_on_large_transaction`: اعلان برای تراکنشهای بزرگ

## راهنمای استفاده برای کاربران

### 1. افزایش موجودی از طریق کارت به کارت
1. وارد بخش "کیف پول" در ربات شوید
2. گزینه "افزایش موجودی" را انتخاب کنید
3. روش "کارت به کارت" را انتخاب کنید
4. شماره کارت مقصد را مشاهده کنید
5. از کارت خود به کارت مقصد انتقال وجه انجام دهید
6. اطلاعات تراکنش را وارد کنید:
   - مبلغ انتقال
   - شماره کارت مبدا
   - نام بانک
   - شماره پیگیری (اختیاری)
7. تراکنش را ارسال کنید
8. منتظر تایید ادمین بمانید

### 2. مشاهده تراکنشها
- وارد بخش "تراکنشها" شوید
- تراکنشهای کیف پول و کارت به کارت را مشاهده کنید
- وضعیت هر تراکنش را بررسی کنید

### 3. مشاهده موجودی
- در صفحه اصلی کیف پول، موجودی فعلی را مشاهده کنید
- موجودی به تومان نمایش داده میشود

## راهنمای استفاده برای ادمینها

### 1. مشاهده تراکنشهای در انتظار
1. وارد پنل ادمین شوید
2. به بخش "تراکنشهای کارت به کارت" بروید
3. تراکنشهای در انتظار را مشاهده کنید
4. جزئیات هر تراکنش را بررسی کنید

### 2. تایید تراکنش
1. روی دکمه "تایید" کلیک کنید
2. شماره پیگیری و شماره مرجع را وارد کنید (اختیاری)
3. یادداشت ادمین را وارد کنید (اختیاری)
4. تایید را کلیک کنید
5. مبلغ به کیف پول کاربر افزوده میشود

### 3. رد تراکنش
1. روی دکمه "رد" کلیک کنید
2. دلیل رد را وارد کنید (اجباری)
3. رد را کلیک کنید
4. دلیل رد به کاربر اطلاع داده میشود

### 4. مشاهده آمار
- در بالای صفحه، آمار کلی تراکنشها را مشاهده کنید
- تعداد تراکنشهای در انتظار، تایید شده و رد شده
- مبالغ کل در انتظار و تایید شده

## عیبیابی و رفع مشکلات

### مشکلات رایج

#### 1. تراکنش ثبت نمیشود
- بررسی کنید که مبلغ در محدوده مجاز باشد
- شماره کارت را بررسی کنید (۱۶ رقم)
- نام بانک را وارد کنید
- اینترنت کاربر را بررسی کنید

#### 2. اعلانها ارسال نمیشوند
- تنظیمات اعلانها را بررسی کنید
- توکن ربات تلگرام را بررسی کنید
- Chat ID ادمین را بررسی کنید

#### 3. موجودی بروزرسانی نمیشود
- اتصال به پایگاه داده را بررسی کنید
- لاگهای خطا را بررسی کنید
- تراکنشها را در پایگاه داده بررسی کنید

#### 4. خطای اعتبارسنجی کارت
- الگوریتم Luhn را بررسی کنید
- فرمت شماره کارت را بررسی کنید
- بانک صادر کننده را بررسی کنید

### لاگهای خطا
تمام خطاها در فایل `logs/wallet_errors.log` ثبت میشوند. برای عیبیابی:

```bash
tail -f logs/wallet_errors.log
```

### لاگهای API
تمام درخواستهای API در پایگاه داده ثبت میشوند. برای بررسی:

```sql
SELECT * FROM logs_api WHERE actions LIKE '%wallet%' ORDER BY time DESC LIMIT 10;
```

## امنیت

### تدابیر امنیتی پیادهشده
1. **اعتبارسنجی ورودیها**: تمام ورودیها اعتبارسنجی میشوند
2. **محدودیت نرخ**: محدودیت در تعداد درخواستها
3. **رمزنگاری**: اطلاعات حساس رمزنگاری میشوند
4. **لاگگیری**: تمام فعالیتها لاگ میشوند
5. **تایید ادمین**: تمام تراکنشها نیاز به تایید ادمین دارند
6. **محدودیت مبلغ**: محدودیت در مبالغ تراکنشها
7. **محدودیت تعداد**: محدودیت در تعداد تراکنشها

### توصیههای امنیتی
1. همیشه از HTTPS استفاده کنید
2. توکنهای API را بهطور منظم تغییر دهید
3. لاگها را بهطور منظم بررسی کنید
4. بروزرسانیهای امنیتی را نصب کنید
5. پشتیبانگیری منظم انجام دهید
6. دسترسیها را محدود کنید

## عملکرد و بهینهسازی

### بهینهسازیهای پیادهشده
1. **ایندکسهای پایگاه داده**: برای جستجوی سریع
2. **کشینگ**: برای کاهش بار پایگاه داده
3. **فشردهسازی**: برای کاهش حجم دادهها
4. **Lazy loading**: برای بارگذاری سریع
5. **CDN**: برای توزیع محتوا

### توصیههای بهینهسازی
1. از Redis برای کشینگ استفاده کنید
2. از CDN برای فایلهای استاتیک استفاده کنید
3. پایگاه داده را بهینه کنید
4. سرور را مانیتور کنید
5. بار تست انجام دهید

## پشتیبانگیری و بازیابی

### پشتیبانگیری خودکار
سیستم میتواند بهطور خودکار پشتیبانگیری انجام دهد:

```bash
# پشتیبانگیری روزانه
0 3 * * * php /path/to/app/wallet/cron/backup.php
```

### بازیابی از پشتیبان
1. فایل پشتیبان را دانلود کنید
2. پایگاه داده را بازیابی کنید
3. فایلها را بازیابی کنید
4. تنظیمات را بررسی کنید

## پشتیبانی و نگهداری

### بروزرسانی سیستم
1. قبل از بروزرسانی پشتیبان بگیرید
2. تغییرات را بررسی کنید
3. تست انجام دهید
4. بروزرسانی را اعمال کنید
5. مانیتور کنید

### مانیتورینگ
- استفاده از ابزارهای مانیتورینگ
- بررسی لاگها
- بررسی عملکرد
- بررسی امنیت

### پشتیبانی فنی
برای پشتیبانی فنی:
- ایمیل: support@mirza-tech.com
- تلگرام: @mirza_support
- تلفن: ۰۲۱-۱۲۳۴۵۶۷۸

## مجوز و حق کپی
این سیستم تحت مجوز MIT منتشر شده است. برای اطلاعات بیشتر به فایل LICENSE مراجعه کنید.

## سپاسگزاری
از تمام کسانی که در توسعه این سیستم مشارکت داشتهاند، سپاسگزاریم.

---
**تاریخ آخرین بروزرسانی:** ۲۰۲۴/۱۲/۲۰
**نسخه:** ۱.۰.۰