#### 📋 راهنمای نصب ربات روی هاست اشتراکی (cPanel / DirectAdmin)

اگر سرور مجازی (VPS) ندارید، می‌توانید ربات را روی هاست‌های اشتراکی نصب کنید.

**۱. دانلود و آپلود:**
*   فایل پروژه را دانلود کنید.
*   وارد **File Manager** هاست شوید.
*   فایل‌ها را در پوشه اصلی سایت (`public_html`) آپلود و اکسترکت کنید.

**۲. ساخت دیتابیس:**
*   **در cPanel:** به بخش `MySQL Databases` بروید. دیتابیس و یوزر بسازید و آنها را به هم متصل کنید (با تیک ALL PRIVILEGES).
*   **در DirectAdmin:** به بخش `MySQL Management` بروید و `Create New Database` را بزنید. (اطلاعات دیتابیس، یوزر و رمز را یادداشت کنید).

**۳. اجرای نصب‌کننده:**
مرورگر خود را باز کنید و به آدرس زیر بروید:
`yourdomain.com/installer` یا `yourdomain.com`

**۴. مراحل نصب:**
*   اطلاعات دیتابیس و اطلاعات ربات (توکن، آیدی عددی) را در فرم وارد کرده و نصب را بزنید.

**۵. تنظیم کرون‌جاب‌ها (بسیار مهم):**
پس از نصب موفق، سیستم لیستی از دستورات را به شما می‌دهد.
*   در هاست خود به بخش **Cron Jobs** بروید.
*   دستورات نمایش داده شده را کپی کرده و اضافه کنید.

### ⏰ لیست دقیق کرون‌جاب‌ها (Cron Jobs)
برای عملکرد بهینه و جلوگیری از کندی هاست، پیشنهاد می‌شود کرون‌جاب‌ها را با زمان‌بندی‌های زیر تنظیم کنید:

| زمان‌بندی (Time) | دستور (Command) | توضیحات |
| :--- | :--- | :--- |
| `* * * * *` | `cronbot/NoticationsService.php` | ارسال نوتیفیکیشن‌ها (هر دقیقه) |
| `*/5 * * * *` | `cronbot/uptime_panel.php` | بررسی وضعیت پنل (هر ۵ دقیقه) |
| `*/5 * * * *` | `cronbot/uptime_node.php` | بررسی وضعیت نودها (هر ۵ دقیقه) |
| `*/10 * * * *` | `cronbot/expireagent.php` | بررسی سرویس‌های منقضی (هر ۱۰ دقیقه) |
| `*/10 * * * *` | `cronbot/payment_expire.php` | بررسی انقضای پرداخت (هر ۱۰ دقیقه) |
| `*/15 * * * *` | `cronbot/iranpay1.php` | بررسی درگاه ریالی (هر ۱۵ دقیقه) |
| `*/15 * * * *` | `cronbot/plisio.php` | بررسی درگاه ارزی (هر ۱۵ دقیقه) |
| `0 * * * *` | `cronbot/statusday.php` | گزارش وضعیت روزانه (هر ساعت) |
| `0 3 * * *` | `cronbot/backupbot.php` | بکاپ خودکار دیتابیس (هر روز ساعت ۳ صبح) |

> **نکته:** در دستورات بالا، باید مسیر کامل فایل PHP را بر اساس هاست خود وارد کنید. اگر از **نصب‌کننده خودکار** استفاده کنید، دستورات کامل به شما داده می‌شود.

**۶. پایان:**
برای امنیت، پوشه `installer` را از هاست خود پاک کنید.

---

**خلاصه:** با خیال راحت فایل‌ها را منتشر کنید. اینستالری که نوشتیم روی سی‌پنل، دایرکت ادمین و حتی پلسک (Plesk) بدون هیچ تغییری در کد کار می‌کند.

### 🔧 عیب‌یابی و مشکلات رایج نصب روی هاست
اگر هنگام نصب روی هاست اشتراکی (cPanel/DirectAdmin) با خطا مواجه شدید، راهنمای زیر را بخوانید:

#### ❌ خطای ۱: `Parse error: syntax error, unexpected ':'` یا خطای Composer
**پیام خطا:**
`Fatal error: Composer detected issues in your platform: Your Composer dependencies require a PHP version ">= 8.2.0".`

**✅ راه حل:**
این ربات با استانداردهای مدرن نوشته شده و **فقط روی PHP 8.1 و 8.2** کار می‌کند.
1. وارد پنل هاست خود شوید.
2. به بخش **Select PHP Version** یا **MultiPHP Manager** بروید.
3. نسخه PHP را روی **8.2** تنظیم کنید.
*(روی هاست‌های رایگان قدیمی که فقط PHP 7.4 دارند، این ربات کار نمی‌کند).*

---

#### ❌ خطای ۲: `Access denied for user ...`
**پیام خطا:**
`اتصال به دیتابیس ناموفق بود: Access denied for user 'user'@'localhost' ...`

**✅ راه حل:**
این یعنی نام دیتابیس یا یوزر را اشتباه وارد کرده‌اید. در هاست‌های اشتراکی، نام دیتابیس همیشه یک **پیشوند** دارد.
*   ❌ **غلط:** `mirzadb`
*   ✅ **درست:** `username_mirzadb` (مثلاً `ali_mirzadb`)

لطفاً نام کامل دیتابیس و نام کاربری را دقیقاً همانطور که در پنل هاست (بخش MySQL Databases) نوشته شده است کپی کنید.

*   **گواهی SSL (بسیار مهم):** دامنه شما حتماً باید دارای گواهی SSL فعال (**HTTPS**) باشد. ربات‌های تلگرام روی آدرس‌های معمولی (HTTP) کار نمی‌کنند. اگر از هاست اشتراکی استفاده می‌کنید، مطمئن شوید که SSL در پنل شما فعال است (یا از Cloudflare استفاده کنید).
    *   *نکته:* از هاست‌های "کاملاً رایگان" (مثل InfinityFree یا Freehost) استفاده نکنید؛ سیستم‌های امنیتی این هاست‌ها معمولاً دسترسی سرورهای تلگرام را مسدود می‌کنند و ربات کار نخواهد کرد.
